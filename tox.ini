[tox]
envlist = py27

[testenv]
usedevelop = true
commands =
    py.test \
        --basetemp={envtmpdir} \
        --cov=spotifyconnect --cov-report=term-missing \
        {posargs}
deps =
    mock
    pytest
    pytest-cov
